# Copy to keystore.properties (gitignored) and fill in to sign release builds.
# Reuse the existing Play upload key so updates to org.jecaro.diverk are accepted.
storeFile=/absolute/path/to/diverk.keystore
storePassword=CHANGME
keyAlias=CHANGEME
keyPassword=CHANGEME
