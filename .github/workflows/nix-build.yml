name: "nix"
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Install nix
      uses: cachix/install-nix-action@v31

    - name: Build diverk
      run: nix build
